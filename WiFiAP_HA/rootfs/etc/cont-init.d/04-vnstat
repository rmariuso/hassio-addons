#!/usr/bin/with-contenv bashio

# Runs the vnstatd 
DIR="/config/wifiap_ha"
DIR_RASPAP="${DIR}/raspap"
CONFIG="${DIR_RASPAP}/vnstat.conf"

# Run lighttpd
bashio::log.info "Starting vnstatd ..."
vnstatd -d --config "${CONFIG}"
vnstat --config "${CONFIG}" -i wlan0
